{
  "name": "task-tracker",
  "version": "1.0.0",
  "description": "A simple vanilla HTML/JS task tracker",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
